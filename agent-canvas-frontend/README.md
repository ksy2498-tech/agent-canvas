# Agent Canvas Frontend

A React 18 + Vite workflow canvas for building agent graphs with ReactFlow, Zustand state, Monaco-powered editors, MCP tool attachment, breakpoints, and graph execution controls.

## Setup

```bash
npm install
npm run dev
```

The app expects an API server at `http://localhost:8000`.
